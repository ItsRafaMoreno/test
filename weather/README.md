## README.md ##

```
$ ansible-playbook -i "localhost" -c local playbooks/docker.yml
```

```
├── Dockerfile
├── ansible
│   ├── ansible.cfg
│   └── playbooks
│       ├── daemon.json
│       └── docker.yml
├── getweather.py
├── index.html
└── requirements.txt
```